---
title: "main"
author: "Nils Brun"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
#load data
library(readr)

data_srf_prep <- read_tsv("../data_prep/data_srf_prep.tsv")
```


```{r}
#Load the stringr package
library(stringr)

#Count the occurrences of "hammas" in each entry of the 'head' column, case insensitive
data_srf$count_hamas <- str_count(tolower(data_srf$head), "hamas")
```







